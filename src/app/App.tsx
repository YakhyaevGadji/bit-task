import {AppWrapper, Container} from "./style/app-style.ts";

const App = () => {
    return(
        <Container>
            <AppWrapper>

            </AppWrapper>
        </Container>
    );
}

export default App;